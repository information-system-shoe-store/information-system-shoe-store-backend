logging:
  level:
    org.springframework.data.r2dbc: DEBUG
spring:
  r2dbc:
    url: r2dbc:mssql://localhost:1433/shoe_store_db
    username: nikitecc
    password: n.lvov_1705
  sql:
    init:
      mode: always
      schema-locations: classpath:sql/schema.sql
      data-locations: classpath:sql/mock.sql